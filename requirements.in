# Add Python dependencies here (unpinned).
# Example:
# langchain
# langgraph
# psycopg[binary]

# Google GenAI SDK (official) for Veo 3.1 first/last frame video generation
google-genai

# YAML parsing (used by scripts and E2E runner)
pyyaml

# Test runner used by CI and local verification
pytest

# Local /line + /image_gen web app
fastapi
uvicorn
line-bot-sdk
