# Tasklist

- [x] `still_image_plan.generation_status` を parse する
- [x] `missing|created|recreate` の runtime 判定を入れる
- [x] `recreate + --force` で既存 canonical を `assets/test/` へ退避する
- [x] `image_generation_requests.md` に全 scene/cut を出す
- [x] request に `still_mode`, `generation_status`, `plan_source` を出す
- [x] docs/template を更新する
- [x] 回帰テストを追加する
