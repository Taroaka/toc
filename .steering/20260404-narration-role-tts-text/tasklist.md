# Tasklist

1. Add `audio.narration.tts_text` support to manifest parsing, generation, and multiagent merge
2. Rework narration evaluator to load `script.md` and score `story_role_fit`
3. Update templates and docs for role-aware evaluation and hiragana `tts_text`
4. Update and run tests
