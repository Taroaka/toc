# Design

- Change CLI default `--image-size` from `2K` to `1K`.
- Update manifest templates and docs that describe default image size.
- Normalize the current Urashima run manifest to `1K` so prompt/export flow matches the default.
