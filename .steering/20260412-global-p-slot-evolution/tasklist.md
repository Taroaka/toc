# Tasklist

- [ ] `Global P-Slot Evolution` の requirement / design / tasklist を追加する
- [ ] 新しい `p-slot` を追加するときの更新対象を docs に明記する
- [ ] fixed `p-slot` contract の正本が `docs/*` と `toc/run_index.py` にあることを明記する
- [ ] `slot.<code>.*` を使って run ごとの差分を表す運用を docs に明記する
- [ ] pointer docs validation を通す
- [ ] 必要なら次段階として `validate-slot-contract.py` の導入を検討する
