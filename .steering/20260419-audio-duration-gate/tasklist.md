# Tasklist

1. `p800` の fixed slot contract を更新する
2. duration review prompt builder を追加する
3. audio duration gate script を追加する
4. `toc-immersive-ride-generate.sh` に gate を組み込む
5. docs / state schema / pointer docs を更新する
6. validator / tests を更新する
