# Requirements

- `assets.character_bible[]` should support optional physical scale metadata for still-generation consistency.
- Still prompts should automatically receive absolute body-scale lines for active characters.
- When multiple characters are active in the same still scene, relative scale rules should also be injected.
- Urashima run should define rideable adult turtle scale so Urashima-to-turtle size remains stable across scene01 and later scenes.
