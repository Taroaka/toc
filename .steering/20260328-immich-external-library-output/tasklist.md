# Tasklist

- [x] Immich managed storage では元の `output/` 階層が出ない方針を確認
- [x] `output/` bind mount を追加する設計を決める
- [x] `immich-setup.sh` を mount 対応に更新する
- [x] 既存環境に mount を反映する補助スクリプトを追加する
- [x] `immich-info.sh` に external library path を表示する
- [x] `immich_remote.md` に External Library 手順を追記する
- [ ] Immich UI で External Library を作成して scan する
