# Tasklist

- [x] `kindle_web_cdp.py` を追加して extractor の reusable primitives を切り出す
- [x] `extract-kindle-web-cdp.py` を新 module 利用に寄せる
- [x] `run-kindle-full-book.py` を追加して per-page export/transcribe/checkpoint loop を実装する
- [x] `run-kindle-full-book.sh` を追加して one-command entrypoint を作る
- [x] `run_state.json` の最小 schema と transcript/session 更新ロジックを実装する
- [x] `--resume` を実装し、中断後に completed pages を保持したまま続行できるようにする
- [x] main path の help と docs を更新する
- [x] syntax / compile verification を通す
