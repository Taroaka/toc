# Tasklist: EvoLink Kling v3/O3 Provider

1. Add `toc/providers/evolink.py` (upload + submit + poll + download).
2. Wire `scripts/generate-assets-from-manifest.py` to route Kling tools to EvoLink when configured.
3. Add minimal docs under `docs/vendor/kling/` describing EvoLink usage and env vars.
4. Run `python -m compileall .` and a dry-run example command.

