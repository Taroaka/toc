# NOTES

## Chronological notes

- 2026-07-10: Existing root `SPEC.md` / `GOAL.md` concern safe parallel image generation and are protected from this goal.
- 2026-07-10: Worktree already contains extensive user changes overlapping server, runner, evaluator, tests, workflow, and docs; preserve and layer changes surgically.
- 2026-07-10: Goal config inspection found Codex CLI 0.144.0, trusted project, goals enabled, approval never, and danger-full-access. The skill's exact autonomous profile differs (newer model selected and long-context keys unset); current task will execute in this active session without editing user config.
- 2026-07-10: Long-form planning keeps the canonical eight-beat order but expands it into target-derived runtime scenes; locations are reused rather than creating one reference asset per expanded scene.
- 2026-07-10: Runtime measurement treats audio and video as parallel timelines. Render units replace source-cut video durations and are never added to them; the pre-render effective duration is the shorter complete timeline.
- 2026-07-10: Existing backend full test baseline currently has nine failures in unrelated prompt-v2/provenance assertions; focused target-duration tests are green. Recheck after integration and do not mask changed-path failures as baseline.
- 2026-07-11: Durable contracts now define `target_duration_seconds` as default 300 / range 300..1200, lower-only 80% gates, parallel audio/video timelines, p740 and final-video state fields, and pre-cut research/story semantic gates. External-source fidelity checks and p680-to-p900 one-click continuation remain explicitly out of scope.
- 2026-07-11: A foundation semantic `passed` report now requires exact scope entry coverage plus five ordered stage-specific criterion results. Every result carries `criterion_id`, `status`, and non-empty artifact-local evidence; missing evidence, failed criteria, and transport failure remain fail-closed.
- 2026-07-11: Backend materialized/no-images validation requires research/story semantic passes even though generated media and later scene/image semantic reports remain optional.
- 2026-07-11: The first real headless attempt proved the repair path is active (research judgment failed and entered producer repair), but it was deliberately interrupted before completion after stricter evidence requirements were identified; it is not completion evidence.
- 2026-07-11: A route-level 1200-second integration test uses the real authoring materializer while mocking only the external semantic transport boundary. It verifies one target across all five artifacts and the state, plus the approved long-form budgets.
- 2026-07-11: Reviewed story refs are authoritative downstream. Event, passage, and conflict refs are resolved against reviewed research instead of falling back to title heuristics; deterministic scene/narration floors are rechecked after review and before cut construction.
- 2026-07-11: Homebrew Codex 0.144.0 was installed without a sibling `codex-code-mode-host`, although an executable host already existed under `$CODEX_HOME/plugins/.plugin-appserver/`. The app-server subprocess environment now discovers that host (or a bundled sibling) via `CODEX_CODE_MODE_HOST_PATH`, preserving an explicit user override.
- 2026-07-11: A reviewer may return a complete machine-readable verdict in its final app-server message instead of writing the report. The transport now accepts only a terminal status plus reviewed/blocked/failed coverage (and foundation criteria JSON), writes that exact AI evidence to the report, and rejects incomplete/error chat. This is transport recovery, not deterministic auto-pass.
- 2026-07-11: The first successful AI research repair exposed a structural hole: semantic content can improve while Markdown/YAML becomes unparsable. Foundation collectors now re-raise parse failure instead of falling back to zero entries, repair prompts require `load_structured_document` round-trip validation, and the initial Cinderella research already contains the roles/conflict/causal handoff that triggered the repair.
- 2026-07-11: Story authoring now reuses the concrete scene blueprint instead of emitting generic motif prose. Cinderella scenes carry explicit location, research refs, character IDs, event obligations, visible action, causal handoff, and narration intent; a 1200-second plan splits the eight canonical scenes into 30 unique, ordered visual responsibilities rather than label-only duplicates.
- 2026-07-11: Semantic repair cannot silently change the request target. Both reviewed research and reviewed story are reparsed and checked against the original duration plan before story/cut authoring. Target and budget fields are present even in in-progress or failed state snapshots.
- 2026-07-11: `output/シンデレラ_20260711_0911` is the final no-paid-media frontless evidence. Research and story passed their real contextless AI reviews on the first attempt, and the backend create route completed at p650 with 300 seconds, 8 scenes, 47 cuts, a 210-second narration budget, and a 564-second planned cut timeline. The upper-bound-free contract intentionally accepts that over-target plan; actual audio/video remain measured later at p740/final media gates.
- 2026-07-11: API request models reject coercible strings/floats for `target_duration_seconds`; only strict JSON integers in 300..1200 pass. Final-video target resolution now follows manifest target, state target, script target, then legacy minimum recovery before the 300-second default, matching the audio gate.
- 2026-07-11: Full runner regression still has two unrelated assertions that require the legacy prompt label `[人物の見える演技]` while current prompt output uses a different character heading. The assertions predate this goal and are outside duration/semantic changes; all 18 relevant runner tests pass.
